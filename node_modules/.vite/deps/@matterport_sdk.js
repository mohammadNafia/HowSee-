import "./chunk-V4OQ3NZ2.js";

// node_modules/@matterport/sdk/dist/index.esm.js
var e;
var t = { d: (e2, s2) => {
  for (var r2 in s2) t.o(s2, r2) && !t.o(e2, r2) && Object.defineProperty(e2, r2, { enumerable: true, get: s2[r2] });
}, o: (e2, t2) => Object.prototype.hasOwnProperty.call(e2, t2) };
var s = {};
t.d(s, { A: () => j, u: () => M });
var r = class {
  constructor(e2) {
    this.targetWindow = e2, this.messageObserver = null, this.onMessage = (e3) => {
      if (this.messageObserver) {
        const t2 = e3.data;
        this.messageObserver.notify(Object.assign({}, t2), { id: t2.fromId, origin: e3.origin, source: e3.source }, e3.data.timestamp);
      }
    };
  }
  listen(e2) {
    this.messageObserver || (this.messageObserver = e2, this.targetWindow.addEventListener("message", this.onMessage));
  }
  stopListening() {
    this.targetWindow.removeEventListener("message", this.onMessage), this.messageObserver = null;
  }
};
var n = class {
  constructor(e2, t2 = "*") {
    this.targetWindow = e2, this.targetOrigin = t2;
  }
  send(e2, t2) {
    this.targetWindow.postMessage(e2, this.targetOrigin);
  }
};
var i = class {
  constructor(e2) {
    this.messageReceiver = e2;
  }
  notify(e2, t2) {
    this.messageReceiver.onMessageReceived(e2, t2);
  }
};
var o = class {
  constructor() {
    this.values = {};
  }
  add(e2, t2) {
    this.getValuesAtKey(e2).push(t2);
  }
  remove(e2, t2) {
    const s2 = this.values[e2];
    if (s2) {
      const e3 = s2.indexOf(t2);
      e3 > -1 && s2.splice(e3, 1);
    }
  }
  removeKey(e2) {
    delete this.values[e2];
  }
  getValuesAtKey(e2) {
    const t2 = this.values[e2] || [];
    return this.values[e2] = t2, t2;
  }
  valuesPerKey(e2) {
    return this.getValuesAtKey(e2).length;
  }
  find(e2, t2) {
    return this.values[e2] && this.values[e2].find(t2);
  }
  get keys() {
    return Object.keys(this.values);
  }
  hasKey(e2) {
    return e2 in this.values;
  }
  has(e2, t2) {
    return this.hasKey(e2) && this.values[e2].includes(t2);
  }
  *[Symbol.iterator]() {
    for (const e2 in this.values) for (const t2 of this.values[e2]) yield [e2, t2];
  }
};
var c = class {
  constructor(e2, t2) {
    this.sourceId = e2, this.listenerBridge = t2, this.observers = new o();
  }
  init() {
    const e2 = new class {
      constructor(e3) {
        this.messenger = e3;
      }
      onMessageReceived(e3, t3) {
        this.messenger.onMessageReceived(e3, t3);
      }
    }(this), t2 = new i(e2);
    this.listenerBridge.listen(t2);
  }
  dispose() {
    this.listenerBridge.stopListening();
  }
  addObserver(e2) {
    this.observers.add(e2.messageType, e2);
  }
  removeObserver(e2) {
    this.observers.remove(e2.messageType, e2);
  }
  send(e2, t2, s2, r2) {
    s2.send(Object.assign(Object.assign({}, e2), { fromId: this.sourceId, toId: t2, timestamp: Date.now() }), r2);
  }
  onMessageReceived(e2, t2) {
    const s2 = e2.type, r2 = this.observers.getValuesAtKey(s2);
    if (r2) for (const s3 of r2) {
      const r3 = e2.payload || e2;
      s3.notify(r3, t2, e2.timestamp);
    }
  }
};
var a = class {
  listen() {
  }
  stopListening() {
  }
  send() {
    throw Error("The sdk has been disconnected and can't make any new calls");
  }
};
var h = class {
  constructor(e2, t2, s2) {
    this.sourceId = e2, this.targetId = t2, this.messageBridge = s2, this.observers = new o();
  }
  static toFilteredMessenger(e2, t2, s2) {
    const r2 = new d(e2.sourceId, e2.targetId, e2, t2);
    for (const t3 of s2) for (const s3 of e2.observers.getValuesAtKey(t3)) r2.addObserver(s3);
    return r2;
  }
  init() {
    const e2 = new class {
      constructor(e3) {
        this.messenger = e3;
      }
      onMessageReceived(e3, t3) {
        this.messenger.onMessageReceived(e3, t3);
      }
    }(this), t2 = new i(e2);
    this.messageBridge.listen(t2);
  }
  dispose() {
    this.messageBridge.stopListening(), this.messageBridge = new a();
  }
  addObserver(e2) {
    this.observers.add(e2.messageType, e2);
  }
  removeObserver(e2) {
    this.observers.remove(e2.messageType, e2);
  }
  send(e2) {
    const t2 = this.sourceId, s2 = this.targetId;
    this.messageBridge.send(Object.assign(Object.assign({}, e2), { fromId: t2, toId: s2, timestamp: Date.now() }));
  }
  onMessageReceived(e2, t2) {
    if (!this.filterMessageId(e2)) return;
    const s2 = e2.type, r2 = this.observers.getValuesAtKey(s2);
    if (r2) for (const s3 of r2) {
      const r3 = e2.payload || e2;
      s3.notify(r3, t2, e2.timestamp);
    }
  }
  filterMessageId(e2) {
    const t2 = e2.toId, s2 = e2.fromId;
    return t2 instanceof Array ? t2.indexOf(this.sourceId) > -1 : (void 0 === t2 || t2 === this.sourceId) && s2 === this.targetId;
  }
};
var d = class extends h {
  constructor(e2, t2, s2, r2) {
    super(e2, t2, new a()), this.messenger = s2, this.sendFilter = r2, this.errorBridge = new a();
  }
  send(e2) {
    this.sendFilter.includes(e2.type) ? this.messenger.send(e2) : this.errorBridge.send();
  }
};
var l = class {
  constructor(e2, t2, s2 = "*") {
    this.sourceWindow = e2, this.listener = new r(e2), this.sender = new n(t2, s2);
  }
  listen(e2) {
    this.listener.listen(e2);
  }
  stopListening() {
    this.listener.stopListening();
  }
  send(e2) {
    this.sender.send(e2, this.sourceWindow);
  }
};
var g = class {
  constructor(e2, t2, s2, r2, n2 = "*") {
    this.sourceId = e2, this.sourceWindow = t2, this.targetId = s2, this.targetWindow = r2, this.targetOrigin = n2;
  }
  createMessenger() {
    const e2 = new l(this.sourceWindow, this.targetWindow, this.targetOrigin);
    return new h(this.sourceId, this.targetId, e2);
  }
};
var u = class {
  constructor() {
    this.libraryCache = /* @__PURE__ */ new Map();
  }
  load(e2, t2) {
    const s2 = this.libraryCache.get(e2) || new m(e2, t2);
    return this.libraryCache.set(e2, s2), s2.library;
  }
};
var m = class {
  constructor(e2, t2) {
    this._libraryPromise = new Promise(((e3, t3) => {
      this.libraryResolver = e3, this.libraryRejecter = t3;
    })), this.fetch(e2, t2);
  }
  async fetch(e2, t2) {
    try {
      const s2 = await import(e2), r2 = Object.assign({}, s2);
      if (t2 && t2 in window) {
        const e3 = window[t2] || {};
        Object.assign(r2, e3), delete window[t2];
      }
      this.libraryResolver(r2);
    } catch (e3) {
      this.libraryRejecter(e3);
    }
  }
  get library() {
    return this._libraryPromise;
  }
};
!(function(e2) {
  const t2 = new u();
  e2.connect = async function(e3, s2, r2) {
    let n2;
    try {
      n2 = await e3.connect();
    } finally {
      e3.cancelConnecting();
    }
    return (function(e4, t3, s3, r3) {
      return new t3(s3, e4).build(r3);
    })(r2, await (async function(e4) {
      if (!e4) throw new Error("Unabled to load the sdk");
      try {
        const s3 = await t2.load(e4, "sdk-client");
        if (s3 && s3.SdkBuilder && "function" == typeof s3.SdkBuilder) return s3.SdkBuilder;
      } catch (e5) {
      }
      throw Error(`Could not load the sdk from ${e4}`);
    })(n2.scriptUrl), s2.getFactory(n2), n2.serializedInterface);
  };
})(e || (e = {}));
var v;
var p;
var y;
!(function(e2) {
  e2.CONNECT = "postmessage.connect";
})(v || (v = {})), (function(e2) {
  e2.HANDSHAKE = "postmessage.handshake", e2.ACCEPT = "postmessage.accept", e2.REJECT = "postmessage.reject";
})(p || (p = {}));
var w = class {
  constructor(e2, t2 = {}) {
    this.type = v.CONNECT, this.payload = { bootstrapVersion: e2, options: { auth: t2.auth, provider: t2.provider, sdkType: t2.sdkType }, applicationKey: t2.applicationKey };
  }
};
var b = class {
  constructor(e2) {
    this.receiver = e2, this.messageType = p.HANDSHAKE;
  }
  notify(e2, t2, s2) {
    this.receiver.handshake();
  }
};
var f = class {
  constructor(e2) {
    this.receiver = e2, this.messageType = p.ACCEPT;
  }
  notify(e2, t2, s2) {
    const { sourceId: r2, scriptUrl: n2, targetId: i2, targetOrigin: o2 } = e2, c2 = e2.interface;
    this.receiver.accept(r2, n2, c2, i2, o2);
  }
};
!(function(e2) {
  e2.CANCELLED = "ConnectionCancelled", e2.REFUSED = "ConnectionRefused", e2.INVALID_PROVIDER = "InvalidProvider", e2.KEY_MISMATCH = "KeyReferrerMismatch";
})(y || (y = {}));
var E = class extends Error {
  constructor() {
    super(), this.type = y.REFUSED, this.name = "ConnectionRefusedError";
  }
};
var O = class extends Error {
  constructor(e2) {
    super(e2), this.type = y.INVALID_PROVIDER, this.name = "InvalidProviderError";
  }
};
var C = class extends Error {
  constructor(e2) {
    super(e2), this.type = y.KEY_MISMATCH, this.name = "KeyReferrerMismatchError";
  }
};
var I = { [y.REFUSED]: E, [y.KEY_MISMATCH]: C, [y.INVALID_PROVIDER]: O };
var S = class {
  constructor(e2) {
    this.receiver = e2, this.messageType = p.REJECT;
  }
  notify(e2, t2, s2) {
    const r2 = I[e2.errorType];
    if (r2) {
      const t3 = new r2(e2.reason);
      this.receiver.reject(t3);
    }
  }
};
var D = class extends Error {
  constructor(e2) {
    super(e2), this.type = y.CANCELLED, this.name = "ConnectionCancelledError";
  }
};
var N;
!(function(e2) {
  e2[e2.IDLE = 0] = "IDLE", e2[e2.CONNECTING = 1] = "CONNECTING", e2[e2.HANDSHAKE = 2] = "HANDSHAKE", e2[e2.CONNECTED = 3] = "CONNECTED", e2[e2.REJECTED = 4] = "REJECTED";
})(N || (N = {}));
var R = class {
  constructor(e2, t2, s2) {
    this.messenger = e2, this.target = t2, this.source = s2, this.connectionState = N.IDLE, this.connectionPoll = void 0;
    const r2 = new class {
      constructor(e3) {
        this.connector = e3;
      }
      handshake() {
        this.connector.handshake();
      }
      accept(e3, t3, s3, r3, n2) {
        this.connector.accept(e3, t3, s3, r3, n2);
      }
      reject(e3) {
        this.connector.reject(e3);
      }
    }(this);
    this.handshakeObserver = new b(r2), this.acceptObserver = new f(r2), this.rejectObserver = new S(r2), this.connectionPromise = new Promise(((e3, t3) => {
      this.resolveConnection = e3, this.rejectConnection = t3;
    }));
  }
  connect(e2, t2 = {}) {
    return this.connectionState === N.IDLE && (this.connectionState = N.CONNECTING, this.messenger.addObserver(this.handshakeObserver), this.messenger.addObserver(this.acceptObserver), this.messenger.addObserver(this.rejectObserver), this.connectionPoll = window.setInterval((() => {
      this.messenger.send(new w(e2, t2), -1, this.target, this.source);
    }), 500)), this.connectionPromise;
  }
  cancelConnecting() {
    this.connectionState < N.CONNECTED && (this.stopConnectPolling(), this.rejectConnection(new D("User manually cancelled connection")));
  }
  handshake() {
    this.connectionState < N.HANDSHAKE && (this.connectionState = N.HANDSHAKE, this.stopConnectPolling(), this.messenger.removeObserver(this.handshakeObserver));
  }
  accept(e2, t2, s2, r2, n2) {
    this.connectionState < N.CONNECTED && (this.connectionState = N.CONNECTED, this.messenger.removeObserver(this.handshakeObserver), this.messenger.removeObserver(this.acceptObserver), this.messenger.removeObserver(this.rejectObserver), this.stopConnectPolling(), this.resolveConnection({ sourceId: e2, targetId: r2, targetOrigin: n2, scriptUrl: t2, serializedInterface: s2 }));
  }
  reject(e2) {
    this.connectionState < N.CONNECTED && (this.connectionState = N.REJECTED, this.messenger.removeObserver(this.handshakeObserver), this.messenger.removeObserver(this.acceptObserver), this.messenger.removeObserver(this.rejectObserver), this.stopConnectPolling(), this.rejectConnection(e2));
  }
  stopConnectPolling() {
    clearInterval(this.connectionPoll), this.connectionPoll = void 0;
  }
};
async function A(e2, t2 = {}) {
  return "string" == typeof t2 ? (function(e3, t3) {
    return console.warn("MP_SDK: connecting using an `applicationKey` argument is deprecated. Please add the key to the iframe's URL parameters instead."), P(e3, { applicationKey: t3 });
  })(e2, t2) : P(e2, t2);
}
function P(t2, s2 = {}) {
  const i2 = (o2 = t2).contentWindow ? o2.contentWindow : null;
  var o2;
  if (!i2) return Promise.reject("invalid window");
  const a2 = Math.floor(1e6 * Math.random()), h2 = new r(window), d2 = new n(i2), l2 = new c(a2, h2);
  l2.init();
  const g2 = new R(l2, d2, window);
  return e.connect(new T(g2, l2, s2), new k(i2), window);
}
var T = class {
  constructor(e2, t2, s2) {
    this.connector = e2, this.postMessage = t2, this.options = s2;
  }
  connect() {
    return this.connector.connect("3.0", this.options);
  }
  cancelConnecting() {
    this.postMessage.dispose();
  }
};
var k = class {
  constructor(e2) {
    this.target = e2;
  }
  getFactory(e2) {
    return new g(e2.sourceId, window, e2.targetId, this.target, e2.targetOrigin);
  }
};
var K = (e2) => "string" == typeof e2 ? document.querySelector(e2) : e2;
var M = async (e2, t2 = {}) => {
  var s2, r2, n2, i2;
  const { connectOptions: o2 = {}, container: c2, domain: a2 = "my.matterport.com", iframe: h2, iframeAttributes: d2 = {}, iframeQueryParams: l2 = new URLSearchParams(), space: g2 = "SxQL3iGyoDo" } = t2;
  let u2 = K(h2);
  if (!e2) throw new Error("sdkKey required to use the SDK.");
  if (!u2) {
    u2 = document.createElement("iframe");
    const t3 = { frameborder: "0", allow: "fullscreen; xr-spatial-tracking; web-share; autoplay", id: "mp-showcase", width: c2 ? "100%" : "853px", height: c2 ? "100%" : "480px" }, r3 = Object.assign(Object.assign({}, t3), d2);
    for (const e3 in r3) u2.setAttribute(e3, r3[e3]);
    const n3 = { m: g2, play: "1", applicationKey: e2 }, i3 = l2 instanceof URLSearchParams ? l2 : new URLSearchParams(Object.entries(l2).map((([e3, t4]) => [e3, t4.toString()])));
    Object.keys(n3).forEach(((e3) => {
      i3.has(e3) || i3.set(e3, n3[e3]);
    }));
    const o3 = a2.replace(/^(?:[^:]*\:\/\/|\/\/)/gm, "");
    u2.setAttribute("src", `//${o3}/show?${i3.toString()}`);
    (null !== (s2 = K(c2)) && void 0 !== s2 ? s2 : await new Promise(((e3) => {
      if (document.body) e3(document.body);
      else {
        const t4 = () => {
          if (document.body) e3(document.body);
          else if ("loading" === document.readyState) setTimeout(t4, 10);
          else {
            const t5 = document.createElement("body");
            document.documentElement.appendChild(t5), e3(t5);
          }
        };
        t4();
      }
    }))).appendChild(u2);
  }
  try {
    return await A(u2, o2);
  } catch (e3) {
    throw (null === (i2 = null === (r2 = e3.toString) || void 0 === r2 ? void 0 : (n2 = r2.call(e3)).includes) || void 0 === i2 ? void 0 : i2.call(n2, "key/referrer mismatch")) && (e3 = `Incorrect SDK key. Did you:
	(a) set up your key using the instructions at https://matterport.github.io/showcase-sdk/sdk_home.html#installation and
	(b) add ${window.location.host} to the allowed domains?

${e3}`), new Error(e3);
  }
};
var j = M;
var L = s.A;
var H = s.u;
export {
  L as default,
  H as setupSdk
};
//# sourceMappingURL=@matterport_sdk.js.map
